"use strict";
// Defining Own Array
// storing favorite transportation names
let transportationMode = ["Car", "MotorCycle", "BiCycle", "Bus"];
// Printing a Statement
for (let i = 0; i < transportationMode.length; i++) {
    console.log(`I Would Like To Own ${transportationMode[i]}`);
}
